<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SajdaApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.SajdaApp.Launch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">260</item>
        <item name="postSplashScreenTheme">@style/Theme.SajdaApp</item>
    </style>
</resources>
